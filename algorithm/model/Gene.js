// gene pool class for each class,faculty,subject,slots
export default class Gene {
    constructor(teacher = "", subject = "") {
        this.teacher = teacher;
        this.subject = subject;
    }
}